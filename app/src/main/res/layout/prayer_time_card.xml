<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="@dimen/_6sdp"
    app:cardCornerRadius="@dimen/_12sdp"
    app:cardElevation="@dimen/_3sdp"
    android:background="@android:color/transparent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/background_gradient"
        android:padding="@dimen/_12sdp">

        <!-- Prayer Name (Replace with specific prayer names for each card instance) -->
        <TextView
            android:id="@+id/prayer_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="FAJR"
            android:textSize="@dimen/_17ssp"
            android:textColor="@android:color/white"
            android:fontFamily="@font/montserrat_bold" />

        <!-- Prayer Time (Replace with specific times for each card instance) -->
        <TextView
            android:id="@+id/prayer_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:text="04:52 am"
            android:textSize="@dimen/_17ssp"
            android:textColor="@android:color/white"
            android:fontFamily="@font/montserrat_regular" />

        <!-- Mute Icon -->
        <ImageView
            android:id="@+id/mute_icon"
            android:layout_width="@dimen/_17sdp"
            android:layout_height="@dimen/_17sdp"
            android:layout_alignParentEnd="true"
            android:layout_marginEnd="@dimen/_6sdp"
            android:layout_below="@id/prayer_time"
            android:src="@drawable/ic_mute"
            app:tint="@android:color/white" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>
